enum Role {
  ADMIN
  HR
  DEPARTMENT_LEAD
  GUARD
}

enum EntryType {
  ENTER
  EXIT
  BOTH
}

enum ActionType {
  PHOTO
  CARD
  CAR
  QR
  PERSONAL_CODE
  ONE_TIME_ID
  USER
}

enum VisitorType {
  EMPLOYEE
  VISITOR
}

enum ActionMode {
  ONLINE
  OFFLINE
}

enum SessionType {
  UNLOCKED
  LOCKED
  LOGIN
  LOGOUT
}

enum VisitorCodeType {
  ONETIME
  MULTIPLE
}


enum StatusEnum {
  WAITING
  PROCESS
  DONE
  FAILED
}

enum ActionStatus {
  ON_TIME
  LATE
  EARLY
  ABSENT
  PENDING
}

enum RuleType {
  USEFUL
  UNUSEFUL
}

enum ResourceType {
  WEBSITE
  APPLICATION
}

enum OptionType {
  WEBSITE
  ACTIVE_WINDOW
}

enum AuthMode {
    FACE
    CARD
    PASSWORD
    CARD_OR_FACE_OR_PASSWORD
    FACE_AND_CARD
    FACE_AND_PASSWORD
    CARD_AND_PASSWORD 
    FINGERPRINT
    FINGERPRINT_OR_CARD
    FINGERPRINT_AND_CARD 
    CARD_OR_FACE 
    CARD_OR_FINGERPRINT_OR_PASSWORD 
}
